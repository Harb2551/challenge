# API
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Core ML (torch 2.4.x to avoid breaking pre-installed torchaudio/torchvision on GPU images)
torch>=2.4.0,<2.5.0
transformers>=4.48.0
safetensors
datasets
scikit-learn
numpy
onnx>=1.14.0
onnxruntime>=1.16.0

# Tokenization & Parsing
sentencepiece
protobuf==3.20.3
tiktoken

# Training Acceleration
accelerate

# Data Generation (Optional for training)
anthropic
python-dotenv
